/**
 * GachaConfig.js - 抽奖系统配置表
 * 纯数据，无逻辑代码
 * 
 * normal: 普通抽（军火商）- 金币
 * premium: 高级抽（黑市）- 钻石，带保底
 * bossDrop: Boss掉落概率（按章节分段）
 * scraps: 分解获得纳米碎片数量
 * solventCost: 焕新试剂价格（钻石/瓶）
 */

module.exports = {

  // ============================================================
  // 普通抽（军火商）
  // ============================================================
  normal: {
    cost: 200,          // 单抽价格（金币）
    costTen: 1800,      // 十连价格（金币，9折）
    currency: 'coins',
    freeDaily: true,    // 每日免费1次
    rates: {
      white:  0.35,
      green:  0.30,
      blue:   0.22,
      purple: 0.10,
      orange: 0.025,
      red:    0.005,
    },
  },

  // ============================================================
  // 高级抽（黑市）
  // ============================================================
  premium: {
    cost: 1,            // 单抽价格（钻石）
    costTen: 9,         // 十连价格（钻石，9折）
    currency: 'diamonds',
    rates: {
      white:  0.15,
      green:  0.25,
      blue:   0.28,
      purple: 0.22,
      orange: 0.08,
      red:    0.02,
    },
    pity: {
      orange: 50,       // 50抽必出橙
      red: 100,         // 100抽必出红
    },
  },

  // ============================================================
  // Boss掉落概率（按章节分段，不掉红色）
  // ============================================================
  bossDrop: [
    { maxChapter: 10,  rates: { white: 0.50, green: 0.30, blue: 0.15, purple: 0.05, orange: 0 } },
    { maxChapter: 30,  rates: { white: 0.30, green: 0.30, blue: 0.25, purple: 0.12, orange: 0.03 } },
    { maxChapter: 60,  rates: { white: 0.10, green: 0.25, blue: 0.35, purple: 0.22, orange: 0.08 } },
    { maxChapter: 100, rates: { white: 0,    green: 0.15, blue: 0.35, purple: 0.35, orange: 0.15 } },
  ],

  // ============================================================
  // 分解获得纳米碎片（5倍递增：5^0, 5^1, 5^2, 5^3, 5^4, 5^5）
  // ============================================================
  scraps: {
    white:  1,
    green:  5,
    blue:   25,
    purple: 125,
    orange: 625,
    red:    3125,
  },

  // ============================================================
  // 焕新试剂价格（洗练消耗）
  // ============================================================
  solventCost: 3,       // 钻石/瓶
};
